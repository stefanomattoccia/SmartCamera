// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.2
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

/***************************** Include Files *********************************/
#include "xsep_convolution_filter.h"

/************************** Function Implementation *************************/
#ifndef __linux__
int XSep_convolution_filter_CfgInitialize(XSep_convolution_filter *InstancePtr, XSep_convolution_filter_Config *ConfigPtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(ConfigPtr != NULL);

    InstancePtr->Axilites_BaseAddress = ConfigPtr->Axilites_BaseAddress;
    InstancePtr->IsReady = XIL_COMPONENT_IS_READY;

    return XST_SUCCESS;
}
#endif

u32 XSep_convolution_filter_Get_kernel_config_V_BaseAddress(XSep_convolution_filter *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE);
}

u32 XSep_convolution_filter_Get_kernel_config_V_HighAddress(XSep_convolution_filter *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (InstancePtr->Axilites_BaseAddress + XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_HIGH);
}

u32 XSep_convolution_filter_Get_kernel_config_V_TotalBytes(XSep_convolution_filter *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return (XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_HIGH - XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + 1);
}

u32 XSep_convolution_filter_Get_kernel_config_V_BitWidth(XSep_convolution_filter *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XSEP_CONVOLUTION_FILTER_AXILITES_WIDTH_KERNEL_CONFIG_V;
}

u32 XSep_convolution_filter_Get_kernel_config_V_Depth(XSep_convolution_filter *InstancePtr) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr->IsReady == XIL_COMPONENT_IS_READY);

    return XSEP_CONVOLUTION_FILTER_AXILITES_DEPTH_KERNEL_CONFIG_V;
}

u32 XSep_convolution_filter_Write_kernel_config_V_Words(XSep_convolution_filter *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_HIGH - XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(int *)(InstancePtr->Axilites_BaseAddress + XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + (offset + i)*4) = *(data + i);
    }
    return length;
}

u32 XSep_convolution_filter_Read_kernel_config_V_Words(XSep_convolution_filter *InstancePtr, int offset, int *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length)*4 > (XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_HIGH - XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(int *)(InstancePtr->Axilites_BaseAddress + XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + (offset + i)*4);
    }
    return length;
}

u32 XSep_convolution_filter_Write_kernel_config_V_Bytes(XSep_convolution_filter *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_HIGH - XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(char *)(InstancePtr->Axilites_BaseAddress + XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + offset + i) = *(data + i);
    }
    return length;
}

u32 XSep_convolution_filter_Read_kernel_config_V_Bytes(XSep_convolution_filter *InstancePtr, int offset, char *data, int length) {
    Xil_AssertNonvoid(InstancePtr != NULL);
    Xil_AssertNonvoid(InstancePtr -> IsReady == XIL_COMPONENT_IS_READY);

    int i;

    if ((offset + length) > (XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_HIGH - XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + 1))
        return 0;

    for (i = 0; i < length; i++) {
        *(data + i) = *(char *)(InstancePtr->Axilites_BaseAddress + XSEP_CONVOLUTION_FILTER_AXILITES_ADDR_KERNEL_CONFIG_V_BASE + offset + i);
    }
    return length;
}

