// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2016.2
// Copyright (C) 1986-2016 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================


`timescale 1 ns / 1 ps

`define TV_IN_filter_V_TDATA "../tv/cdatafile/c.convolution_filter.autotvin_filter_V.dat"

`define AUTOTB_TRANSACTION_NUM 1

module AESL_axi_s_filter_V (
    input clk,
    input reset,
    output [8 - 1:0] TRAN_filter_V_TDATA,
    output TRAN_filter_V_TVALID,
    input TRAN_filter_V_TREADY,
    input ready,
    input done,
    output [31:0] transaction);

    wire TRAN_filter_V_TVALID_temp;
    wire filter_V_TDATA_full;
    wire filter_V_TDATA_empty;
    reg filter_V_TDATA_write_en;
    reg [8 - 1:0] filter_V_TDATA_write_data;
    reg filter_V_TDATA_read_en;
    wire [8 - 1:0] filter_V_TDATA_read_data;
    
    fifo #(11, 8) fifo_filter_V_TDATA (
        .reset(1'b0),
        .write_clock(clk),
        .write_en(filter_V_TDATA_write_en),
        .write_data(filter_V_TDATA_write_data),
        .read_clock(clk),
        .read_en(filter_V_TDATA_read_en),
        .read_data(filter_V_TDATA_read_data),
        .full(filter_V_TDATA_full),
        .empty(filter_V_TDATA_empty));
    
    always @ (*) begin
        filter_V_TDATA_write_en <= 0;
        filter_V_TDATA_read_en <= TRAN_filter_V_TREADY & TRAN_filter_V_TVALID;
    end
    
    assign TRAN_filter_V_TDATA = filter_V_TDATA_read_data;
    assign TRAN_filter_V_TVALID = TRAN_filter_V_TVALID_temp;

    
    assign TRAN_filter_V_TVALID_temp = ~(filter_V_TDATA_empty);
    
    function is_blank_char(input [7:0] char);
        if (char == " " || char == "\011" || char == "\012" || char == "\015") begin
            is_blank_char = 1;
        end else begin
            is_blank_char = 0;
        end
    endfunction
    
    function [151:0] read_token(input integer fp);
        integer ret;
        begin
            read_token = "";
                    ret = 0;
                    ret = $fscanf(fp,"%s",read_token);
        end
    endfunction
    
    function [151:0] rm_0x(input [151:0] token);
        reg [151:0] token_tmp;
        integer i;
        begin
            token_tmp = "";
            for (i = 0; token[15:0] != "0x"; token = token >> 8) begin
                token_tmp = (token[7:0] << (8 * i)) | token_tmp;
                i = i + 1;
            end
            rm_0x = token_tmp;
        end
    endfunction
    
    reg [31:0] transaction_load_filter_V_TDATA;
    
    assign transaction = transaction_load_filter_V_TDATA;
    
    initial begin : AXI_stream_driver_filter_V_TDATA
        integer fp;
        reg [151:0] token;
        reg [8 - 1:0] data;
        reg [8 * 5:1] str;
        integer ret;
        
        transaction_load_filter_V_TDATA = 0;
        fifo_filter_V_TDATA.clear();
        wait (reset === 1);
        fp = $fopen(`TV_IN_filter_V_TDATA, "r");
        if (fp == 0) begin
            $display("ERROR: Failed to open file \"%s\"!", `TV_IN_filter_V_TDATA);
            $finish;
        end
        token = read_token(fp);
        if (token != "[[[runtime]]]") begin
            $display("ERROR: token %s != [[[runtime]]]", token);
            $finish;
        end
        token = read_token(fp); // read 1st "[[transaction]]"
        forever begin
            @ (negedge clk);
            if (ready == 1) begin
                if (token != "[[[/runtime]]]") begin
                    if (token != "[[transaction]]") begin
                        $display("ERROR: token %s != [[[transaction]]]", token);
                        $finish;
                    end
                    token = read_token(fp); // skip transaction number
                    fifo_filter_V_TDATA.clear();
                    token = read_token(fp);
                    while (token != "[[/transaction]]") begin
                        if (fifo_filter_V_TDATA.full) begin
                            $display("ERROR: FIFO is full");
                            $finish;
                        end
                        ret = $sscanf(rm_0x(token), "%x", data);
                        fifo_filter_V_TDATA.push(data);
                        token = read_token(fp);
                    end
                    token = read_token(fp);
                    fifo_filter_V_TDATA.snapshot();
                end else begin
                    fifo_filter_V_TDATA.restore();
                    if (fp != 0) begin
                        $fclose(fp);
                        fp = 0;
                    end
                end
                transaction_load_filter_V_TDATA = transaction_load_filter_V_TDATA + 1;
            end
        end
    end

endmodule
